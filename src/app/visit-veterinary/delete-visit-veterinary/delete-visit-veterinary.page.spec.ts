import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DeleteVisitVeterinaryPage } from './delete-visit-veterinary.page';

describe('DeleteVisitVeterinaryPage', () => {
  let component: DeleteVisitVeterinaryPage;
  let fixture: ComponentFixture<DeleteVisitVeterinaryPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DeleteVisitVeterinaryPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
