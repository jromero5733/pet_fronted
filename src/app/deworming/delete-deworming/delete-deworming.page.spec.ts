import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DeleteDewormingPage } from './delete-deworming.page';

describe('DeleteDewormingPage', () => {
  let component: DeleteDewormingPage;
  let fixture: ComponentFixture<DeleteDewormingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DeleteDewormingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
